syntax on

set tabstop=4
set shiftwidth=4
set expandtab

" Do not save a backup
set nobackup
set nowritebackup

" Show line number
set nu

" Set to utf-8
set encoding=utf-8

" Configure Netrw with tree view
let g:netrw_liststyle=3

" Show trailing whitespaces
match ErrorMsg '\s\+$'
